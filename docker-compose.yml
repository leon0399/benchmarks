version: "3.8"

services:
  benchmark:
    image: ghcr.io/leon0399/benchmark:latest
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    working_dir: /app
    volumes:
      - ./:/app
