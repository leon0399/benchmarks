title: 'Java'

strategy:
  matrix:
    command:
      - title: 'Naive'
        command: 'java -jar %s.jar'
      - title: 'Naive (GraalVM)'
        command: 'graalvm.java -jar %s.jar'

    files:
      - primes/Simple
      - primes/Atkin

      - collatz/MaxSequence

      - mandelbrot/Simple

      - io/Load
      - io/NoLoad
