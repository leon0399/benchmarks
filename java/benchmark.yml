title: 'Java'

strategy:
  matrix:
    command:
      - title: 'Naive'
        command: 'java -jar %s.jar'
      - title: 'GraalVM: Naive'
        command: 'graalvm.java -jar %s.jar'
        tags:
          - GraalVM

    files:
      - primes/Simple
      - primes/Atkin

      - collatz/MaxSequence

      - mandelbrot/Simple

      - treap/Naive

      - io/Load
      - io/NoLoad
