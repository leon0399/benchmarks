title: 'Java'

strategy:
  matrix:
    command:
      - title: 'Java'
        command: 'java -jar %s.jar'
      - title: 'GraalVM'
        command: 'graalvm.java -jar %s.jar'
        tags:
          - GraalVM

    files:
      - primes/Simple
      - primes/Atkin

      - collatz/MaxSequence

      - mandelbrot/Simple

      - recursion/Tak

      - treap/Naive

      - linpack/Linpack
