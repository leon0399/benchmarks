title: 'JavaScript'

strategy:
  matrix:
    command:
      - title: 'Node'
        command: 'node %s'
      - title: 'GraalVM Node'
        command: 'graalvm.node %s'

    files:
      - primes/Simple.js
      - primes/Atkin.js

      - collatz/MaxSequence.js

      - mandelbrot/Simple.js

      - io/Load.js
      - io/NoLoad.js
