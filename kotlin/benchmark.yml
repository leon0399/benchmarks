title: 'Kotlin'

strategy:
  matrix:
    command:
      - title: 'JVM'
        command: 'java -jar %s.jar'
      - title: 'Native'
        command: './%s.kexe'

    files:
      - primes/Simple

      - treap/Naive
