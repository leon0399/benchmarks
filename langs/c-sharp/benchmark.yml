title: 'C#'

strategy:
  matrix:
    command:
      - title: 'csc'
        command: 'mono {file}-csc.exe'

      - title: 'mcs'
        command: 'mono {file}-mcs.exe'

    files:
      - primes/Simple
      - primes/Atkin

      - collatz/MaxSequence

      - treap/Naive

      - recursion/Tak

      - mandelbrot/Simple

      - linpack/Linpack
