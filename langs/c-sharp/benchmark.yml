title: 'C#'

strategy:
  matrix:
    command:
      - title: 'csc'
        command: 'mono %s-csc.exe'

      - title: 'mcs'
        command: 'mono %s-mcs.exe'

    files:
      - primes/Simple
      - primes/Atkin

      - collatz/MaxSequence

      - treap/Naive

      - recursion/Tak

      - mandelbrot/Simple

      - linpack/Linpack
