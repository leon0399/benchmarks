title: 'C'

strategy:
  matrix:
    command:
      - title: 'clang'
        command: './{file}-clang'
      - title: 'gcc'
        command: './{file}-gcc'

    files:
      - primes/Simple
      - recursion/Tak
