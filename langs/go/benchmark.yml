title: 'Go'

strategy:
  matrix:
    command:
      - title: 'Go'
        command: './%s'

    files:
      - primes/Simple
      - primes/Atkin

      - collatz/MaxSequence

      - mandelbrot/Simple

      - recursion/Tak

      - linpack/Linpack

      - treap/Naive
