title: 'Java'

strategy:
  matrix:
    command:
      - title: 'Java'
        command: 'java -jar {file}.jar'
      - title: 'GraalVM'
        command: 'graalvm.java -jar {file}.jar'
        tags:
          - GraalVM

    files:
      - primes/Simple
      - primes/Atkin

      - collatz/MaxSequence

      - mandelbrot/Simple

      - recursion/Tak

      - treap/Naive

      - linpack/Linpack
