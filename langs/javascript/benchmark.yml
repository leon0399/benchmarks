title: 'JavaScript'

strategy:
  matrix:
    command:
      - title: 'Node'
        command: 'node {file}'
      - title: 'GraalVM'
        command: 'graalvm.node {file}'
        tags:
          - GraalVM

    files:
      - linpack/Linpack.js

      - primes/Simple.js
      - primes/Atkin.js

      - collatz/MaxSequence.js

      - mandelbrot/Simple.js

      - treap/Naive.js

      - recursion/Tak.js
