title: 'JavaScript'

strategy:
  matrix:
    command:
      - title: 'Node'
        command: 'node %s'
      - title: 'GraalVM'
        command: 'graalvm.node %s'
        tags:
          - GraalVM

    files:
      - linpack/Linpack.js

      - primes/Simple.js
      - primes/Atkin.js

      - collatz/MaxSequence.js

      - mandelbrot/Simple.js

      - treap/Naive.js

      - recursion/Tak.js
