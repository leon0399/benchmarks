title: 'Kotlin'

strategy:
  matrix:
    versions:
      - version: 1.9
        tags:
          - Old
      - version: 2.0
        tags:
          - Old
      - version: 2.1

    command:
      - title: 'JVM'
        command: 'java -jar {file}.{version}.jar'

      - title: 'Native'
        command: './{file}.{version}.kexe'

    files:
      - primes/Simple

      - treap/Naive
