title: 'Kotlin'

strategy:
  matrix:
    command:
      - title: 'JVM'
        command: 'java -jar {file}.jar'
      - title: 'Native'
        command: './{file}.kexe'

    files:
      - primes/Simple

      - treap/Naive
