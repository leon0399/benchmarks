title: 'Lua'

strategy:
  matrix:
    command:
      - title: 'Lua'
        command: 'lua {file}'
      - title: 'LuaJIT'
        command: 'luajit {file}'
        tags:
          - JIT

    files:
      - primes/Simple.lua
      - treap/Naive.lua
      - recursion/Tak.lua
      - mandelbrot/Simple.lua
