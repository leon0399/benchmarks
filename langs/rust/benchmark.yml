title: 'Rust'

strategy:
  matrix:
    command:
      - title: 'Rust'
        command: './{file}'

    files:
      - primes/Simple
      - primes/Atkin

      - collatz/MaxSequence

      - mandelbrot/Simple
      - linpack/Linpack
