title: 'Swift'

strategy:
  matrix:
    command:
      - title: 'Swift'
        command: 'swift {file}.swift'

      - title: 'Optimized'
        command: 'swift -O {file}.swift'

      - title: 'Compiled'
        command: './{file}'

    files:
      - primes/Simple
      - primes/Atkin
