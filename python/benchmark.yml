title: 'Python'

strategy:
  matrix:
    command:
      - title: 'CPython'
        command: 'python3 %s.py'
      - title: 'PyPy'
        command: 'pypy3 %s.py'
        tags:
          - Alternative

    files:
      - linpack/Linpack

      - primes/Atkin

      - treap/Naive

      - recursion/Tak
      - primes/Simple
      - collatz/MaxSequence
      - mandelbrot/Simple
