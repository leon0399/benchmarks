title: Rust

strategy:
  matrix:
    command:
      - title: Rust
        command: ./%s
      - title: Rust Linpack
        command: ./%s

      - primes/Simple
      - primes/Atkin
      - collatz/MaxSequence
      - mandelbrot/Simple
      - io/Load
      - io/NoLoad
      - linpack/Linpack
      - primes/Simple
      - primes/Atkin

      - collatz/MaxSequence

      - mandelbrot/Simple

      - io/Load
      - io/NoLoadtitle: 'Rust'

strategy:
  matrix:
    command:
      - title: 'Rust'
        command: './%s'
      - title: 'Rust Linpack'
        command: './%s'

    files:
      - primes/Simple
      - primes/Atkin

      - collatz/MaxSequence

      - mandelbrot/Simple

      - io/Load
      - io/NoLoad
    files:
      - primes/Simple
      - primes/Atkin

      - collatz/MaxSequence

      - mandelbrot/Simple

      - io/Load
      - io/NoLoad
=======
