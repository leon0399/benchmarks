title: "Rust"

strategy:
  matrix:
    command:
      - title: "Rust"
        command: "./%s"

    files:
      - primes/Simple
      - primes/Atkin

      - collatz/MaxSequence

      - mandelbrot/Simple

      - io/Load
      - io/NoLoad
      - linpack/Linpack
