title: 'Rust'

strategy:
  matrix:
    command:
      - title: 'Rust'
        command: './%s'
        
    benchmark:
      - primes/Simple
      - primes/Atkin

      - collatz/MaxSequence

      - mandelbrot/Simple  
      - linpack/Linpack

    files:
      - primes/Simple
      - primes/Atkin

      - collatz/MaxSequence 

      - mandelbrot/Simple
